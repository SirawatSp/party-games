# AGENTS.md — คู่มือสำหรับ AI agent ที่มาช่วยงานเว็บนี้

เว็บรวมเกมปาร์ตี้ภาษาไทย เป็นเว็บ static ล้วน **ไม่มี build step ไม่มี dependency**
เปิดไฟล์ `.html` ตรง ๆ ก็ทำงานได้ Deploy ด้วย GitHub Pages ผ่าน `.github/workflows/deploy-pages.yml`
ทุกครั้งที่ push ขึ้น `master`

งานที่พบบ่อยที่สุดคือ **เพิ่มโจทย์เข้าคลังเกม** ซึ่งมีคู่มือละเอียดอยู่ที่
[`docs/CONTENT-GUIDE.md`](docs/CONTENT-GUIDE.md) — อ่านก่อนเริ่มเสมอ

---

## กติกาที่ห้ามพลาด

1. **ตรวจก่อน commit ทุกครั้ง** — `node scripts/check-content.js` ต้องไม่มี ERROR
2. **เลื่อน `CACHE_VERSION` ใน `sw.js` ทุกครั้งที่แก้เนื้อหา** — ไม่งั้นคนที่เคยเปิดเว็บไว้
   จะไม่เห็นของใหม่เลย เพราะ service worker เสิร์ฟไฟล์เก่าจาก cache
   (`node scripts/ship.js` ทำให้อัตโนมัติ)
3. **รัน `node scripts/build-game-counts.js` ทุกครั้งที่จำนวนโจทย์เปลี่ยน** —
   ไม่งั้นป้ายจำนวนบนหน้าแรกจะค้างเลขเก่า (`ship.js` ทำให้เหมือนกัน)
4. **ภาษาไทยทั้งหมด** — ทั้งโจทย์ คอมเมนต์ในโค้ด และข้อความ commit
5. **ห้ามใส่ชื่อรุ่นโมเดล AI** ลงใน commit message, โค้ด, หรือไฟล์ใด ๆ ใน repo
6. **ห้ามเปิด pull request เว้นแต่เจ้าของสั่ง** — งานปกติให้ commit ลง `master` ตรง ๆ

## คำสั่งที่ใช้จริง

```bash
node scripts/check-content.js                     # ตรวจคลังโจทย์ทั้ง 22 คลัง
node scripts/check-content.js flashquiz --new 20  # ตรวจคลังเดียว โชว์ REVIEW เฉพาะ 20 ข้อท้าย
node scripts/add-entries.js flashquiz new.json    # ต่อโจทย์ใหม่เข้าท้ายคลังอย่างปลอดภัย
node scripts/ship.js                              # ตรวจ + สร้างป้ายจำนวน + เลื่อน cache รวดเดียว
python3 -m http.server 8099 --bind 127.0.0.1      # เปิดดูเว็บจริงที่ http://127.0.0.1:8099
node scripts/smoke-test.js                        # เปิดทุกหน้าด้วยเบราว์เซอร์จริง เช็ก JS error + เกมหลัก (ต้องมี Playwright)
```

## โครงไฟล์

```
*.html            หนึ่งไฟล์ต่อหนึ่งเกม (29 หน้า) + index.html เป็นหน้ารวม
js/<เกม>.js       ตรรกะของเกมนั้น ๆ  ·  js/main.js มีตัวสุ่มกลาง createPicker()
data/<เกม>.js     คลังโจทย์ เขียนเป็น const ARRAY_NAME = [...]  ไม่มี export
css/style.css     สไตล์ทั้งเว็บไฟล์เดียว
scripts/          เครื่องมือ dev รันด้วย node เปล่า ไม่มี dependency
sw.js             service worker — มี CACHE_VERSION ที่ต้องเลื่อนทุก deploy
```

## เรื่องที่เคยพลาดมาแล้ว อย่าพลาดซ้ำ

- **คลังบางไฟล์ข้อสุดท้ายไม่มีจุลภาคปิดท้าย** ถ้าต่อข้อใหม่ตรง ๆ จะได้ `} { question:` แล้วไฟล์พังทั้งไฟล์
  → ใช้ `scripts/add-entries.js` ซึ่งเติมจุลภาคให้เอง
- **การหาโจทย์ซ้ำด้วยการเทียบข้อความตรง ๆ ไม่พอ** คู่ที่ถามกลับด้านกันจะรอดไปได้
  (เช่น "เตตริสมีเป้าหมายอะไร" กับ "เกมใดเรียงบล็อกให้เต็มแถว") → อ่าน REVIEW ที่ validator พ่นออกมาทุกบรรทัด
- **ชื่อทับศัพท์สะกดได้หลายแบบ** "นาร์วาฬ" กับ "นาร์วาล" เคยหลุดเป็นสองข้อจากการเพิ่มคนละวัน
  → grep ทุกแบบสะกด และ grep คำตอบด้วย ไม่ใช่แค่คำถาม
- **Flash Quiz คือเกมหลักของเว็บ และเป็นเกม "ตอบไว"** คำตอบไม่เกิน 40 ตัวอักษร ระดับความยากที่คนในวง 1 ใน 3 ตอบได้
  ดูกฎเฉพาะใน CONTENT-GUIDE หัวข้อ 2
- **คำตอบซ้ำกันไม่ได้แปลว่าคำถามซ้ำ** ประเทศ ศิลปิน หรือปี ใช้เป็นคำตอบร่วมกันได้หลายข้อโดยชอบธรรม
  validator จึงแค่รายงานให้ดู ไม่บล็อกให้
- **เพิ่มหมวดใหม่ในคลัง แต่ลืมเพิ่มปุ่มกรองในหน้าเว็บ** → โจทย์นั้นจะไม่มีวันถูกสุ่มเจอในโหมดกรอง
  validator จับให้แล้ว แต่ต้องไปแก้ทั้ง `<เกม>.html` และ `js/<เกม>.js`
